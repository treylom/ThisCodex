# Runtime deps for the reference bridge (examples/bot.py).
# Install: python3 -m pip install -r requirements.txt
#
# Ubuntu 24.04+/Debian refuse system-python installs (PEP 668,
# "externally-managed-environment"). Working paths, measured 2026-08-09 WSL:
#   a) python3 -m pip install --break-system-packages -r requirements.txt
#      (system python — the launcher's default interpreter sees it), or
#   b) uv venv && install there, then set THISCODEX_PYTHON=.venv/bin/python
#      before run.sh — the launcher only runs $THISCODEX_PYTHON (or python3);
#      activating a venv in your shell does NOT reach the tmux windows.
# Note: plain `python3 -m venv` can yield a pip-less venv when the
# python3-venv package is missing.
discord.py>=2.3
websockets>=12
